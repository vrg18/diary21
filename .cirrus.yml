container:
  image: cirrusci/flutter:1.25.0-8.3.pre

#test_task:
#  pub_cache:
#    folder: ~/.pub-cache
#  test_script: flutter test

build_task:
  build_script:
    - flutter config --enable-web
    - flutter build web --no-sound-null-safety --release
  binaries_artifacts:
    path: "build/web/**"
